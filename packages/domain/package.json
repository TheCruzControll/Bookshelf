{
  "name": "@hone/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run --coverage",
    "build": "tsc --emitDeclarationOnly"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}

